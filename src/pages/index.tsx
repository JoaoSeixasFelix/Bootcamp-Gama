import type { NextPage } from "next";
import LadingPage from "./ladingpage";
import SignIN from "./signin";

const Home: NextPage = () => {
  return (
    <div>
      <SignIN />
    </div>
  );
};

export default Home;
